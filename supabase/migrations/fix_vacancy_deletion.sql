-- Quick fix to make vacancy_id nullable
-- This is required for ON DELETE SET NULL to work properly

-- Make vacancy_id nullable
ALTER TABLE public.job_applications
ALTER COLUMN vacancy_id DROP NOT NULL;

-- Verify the constraint is correct
-- You should see ON DELETE SET NULL in the result
SELECT
    tc.constraint_name,
    tc.table_name,
    kcu.column_name,
    ccu.table_name AS foreign_table_name,
    ccu.column_name AS foreign_column_name,
    rc.delete_rule
FROM information_schema.table_constraints AS tc
JOIN information_schema.key_column_usage AS kcu
    ON tc.constraint_name = kcu.constraint_name
    AND tc.table_schema = kcu.table_schema
JOIN information_schema.constraint_column_usage AS ccu
    ON ccu.constraint_name = tc.constraint_name
    AND ccu.table_schema = tc.table_schema
JOIN information_schema.referential_constraints AS rc
    ON rc.constraint_name = tc.constraint_name
WHERE tc.constraint_type = 'FOREIGN KEY'
    AND tc.table_name = 'job_applications'
    AND kcu.column_name = 'vacancy_id';

